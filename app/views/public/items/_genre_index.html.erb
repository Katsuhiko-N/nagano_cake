<table>
    <thead>
        <tr>
            <th>ジャンル検索</th>
        </tr>
    </thead>
    <tbody>
        <% genres.each do |genre| %>
        <tr>
            <td><p><%= link_to "#{genre.name}", items_path(genre_name: genre.name) %></p></td>
        </tr>
        <% end %>
    </tbody>
</table>